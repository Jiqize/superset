# Phase 3L safe evidence

This directory contains fixture-only, sanitized acceptance evidence for Archive
Intent v0.1. It excludes credentials, absolute host paths, raw Workspace,
terminal, runtime, native-session, and epoch identifiers, and all terminal
prompts/transcripts.

Evidence index:

- `archive-mixed-1440x800.png` — eight archived rows across live Pi,
  resumable Pi, unavailable Pi, and Superset CLI `UNTRACKED` evidence, plus a
  real changed-file count and ten durable Work Folders at 1440×800;
- `archive-mixed-1920x976.png` — the same mixed Archived projection at
  1920×976;
- `archived-resume-surface-1920x976.png` — the existing exact Pi resume action
  remains available while the Work Folder is archived;
- `acceptance-summary.json` — sanitized migration, runtime-invariance,
  restart/resume, interaction, viewport, and regression facts.

The screenshots are limited to synthetic AA surfaces. No terminal viewport or
private repository content is included.
